# Quantum Execution Time Prediction — Dependencies
# Python 3.9 (recommended, tested with 3.9.25)
#
# Quick install (CPU only):
#   pip install -r requirements.txt
#
# GPU training (NVIDIA CUDA 11.8+):
#   pip install torch==2.11.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   pip install torch-geometric==2.7.0
#   pip install qiskit-aer-gpu   # replaces qiskit-aer for GPU-accelerated simulation
#   pip install -r requirements.txt --ignore-requires-python
#
# See README.md for full hardware requirements and setup guide.

# ── Quantum computing ──────────────────────────────────────────────────────────
qiskit==2.3.1
qiskit-aer==0.17.2              # CPU simulation; replace with qiskit-aer-gpu for GPU
qiskit-ibm-runtime==0.46.1

# ── Deep learning ─────────────────────────────────────────────────────────────
torch==2.11.0                   # CPU build; see GPU note above
torchvision==0.26.0
torchaudio==2.11.0
torch-geometric==2.7.0
torchpack==0.3.1

# ── Data science ──────────────────────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.6.1
msgpack-numpy==0.4.8

# ── Utilities ─────────────────────────────────────────────────────────────────
networkx>=3.2.0
joblib>=1.3.0
